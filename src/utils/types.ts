/**
 * Shared border-radius preset for all dosieungon components.
 *   none  → 0
 *   sm    → var(--dsg-radius-sm)   6px
 *   md    → var(--dsg-radius)      8px
 *   lg    → var(--dsg-radius-lg)  12px
 *   full  → var(--dsg-radius-full) pill
 */
export type Rounded = 'none' | 'sm' | 'md' | 'lg' | 'full';
